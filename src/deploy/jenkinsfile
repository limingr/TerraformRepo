pipeline {
    agent any
    
    environment {
        AZURE_SUBSCRIPTION_ID='aae27198-c02d-49fc-a164-d45d35267dd2'
        AZURE_TENANT_ID='e22100bd-861d-4f4c-a190-7c4f45feeccf'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building....'
                sh 'pwd'
            }
        }
        stage('Test') {
            steps {
                withCredentials([azureServicePrincipal('AzureServicePrincipal')]) {
                    dir('.'){
/*                         sh 'az login --service-principal -u $AZURE_CLIENT_ID -p $AZURE_CLIENT_SECRET -t $AZURE_TENANT_ID'
                        sh 'az account set -s $AZURE_SUBSCRIPTION_ID' */
                        sh """#!/bin/bash
                        set -e
                        terraform workspace new wsdeploy2 || true
                        terraform workspace select wsdeploy2
                        terraform init -no-color \
                            -backend-config='storage_account_name=tstate13126' \
                            -backend-config='container_name=tstate' \
                            -backend-config='key=terraform.mytfstate' \
                            -backend-config='access_key=99nPCKyui87OegRjeSTYFmcNy/mMNoI3VFCn9CUWiKwTM3cR+e5NeL10ZareLxlmEqvrNEGgMLP816VsDJHa4Q=='
                            
                        terraform plan -no-color -var client_secret='${AZURE_CLIENT_SECRET}' -out=deployCodePlan
                        terraform apply -no-color deployCodePlan
                        sh """
                    }
                }
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
